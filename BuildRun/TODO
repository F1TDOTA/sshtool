1. 支持钉钉发消息 -ok
2. 客户端开发脚本(myscp.sh) -ok
3. 支持界面配置（WEB还是UI都行），统一管理SSH -ok
7. 支持界面一键打开securecrt,xshell,moba(预研，securecrt应该可以，其它不一定行） -ok
4. 监控本地文件并上传 -ok
5. 支持快捷键执行远程编译命令，传递文件（自定义），然后停留在SSH界面 --暂时不搞，看前面的效果
6. 在5的基础上，支持自定义配置集合，并支持快捷键. --暂时不搞，看前面的效果
----------------------------------------------------------
1. 增加复制密码功能 -ok
3. 监控的时候有目录的同步出错了- bug -ok
4. 监控上传服务列表不全 - bug -ok 看错了
5. 监控要增加一个开关（开启后如何关闭问题） - 清掉上传服务器就行了 -ok
2. 监控的时候开始的时候同步一次  -ok
6. 下载的时候要支持下载到本地  -ok

----------------------------------------------------------
